@css{

    .bubbletext:after {
        content: "";
        position: absolute;
        width: 0;
        height: 0;
        border-top: 50px solid #eee;
        left: 60%;
        border-right: 30px solid transparent;
        transform: rotate(5deg) translateX(-50%);
    }

}